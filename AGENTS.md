# AGENTS.md — журнал для всех ИИ-агентов

> **Над проектом Jaqyn работает несколько ИИ-агентов (Codex, Claude и др.).**
> Этот файл — единая точка координации. Прочитай его в начале работы и
> **обязательно дополняй в конце своей сессии.**

## ⛏ Правила (читать перед работой)

1. **Любое изменение — записывай в раздел «Журнал изменений» ниже.** Одна строка =
   одно осмысленное изменение. Указывай: дату, кто ты (агент), что сделал, какие
   файлы затронул, статус (сделано / в работе).
2. **Новые записи — сверху** (свежее первым), под заголовком сессии с датой.
3. **Идеи на будущее** не реализуй сразу — добавляй в раздел «Идеи на будущее»
   со статусом `💡 предложено`. Реализовывать можно только после того, как
   **владелец проекта одобрит** (`✅ одобрено`).
4. Не дублируй: подробный технический дневник — в `PROGRESS.md`, план — в `PLAN.md`,
   деплой — в `DEPLOY.md` / `FREE_TEST_DEPLOY.md`. Здесь — **кто что менял** и **идеи**.
5. Не пиши в журнал секреты (токены, ключи, пароли).

**Легенда статусов идей:** `💡 предложено` · `✅ одобрено` · `🚧 в работе` · `✔️ сделано` · `❌ отклонено`

---

## 📜 Журнал изменений

### 2026-06-18 — Claude (Opus 4.8)

- **Деплой подключён.** Supabase Postgres (session pooler) → Render (Docker, free) →
  Vercel (Mini App). Бэкенд живой: `https://jaqyn-dating-bot.onrender.com`,
  фронт: `https://jaqyn-dating-bot.vercel.app`. _Сделано._
- **fix:** исправлен revision ID в первой миграции (`0001` → `0001_initial`), цепочка
  Alembic больше не падает. `db/migrations/versions/0001_initial.py`. _Сделано._
- **fix:** добавлен корневой `vercel.json` (сборка `mini_app`) + env `VITE_API_URL`.
  `vercel.json`. _Сделано._
- **feat (UX):** теплее главный экран, кнопки ленты с иконками+подписями, золотой
  блок «Совпадает с тобой», подпись на сплэше, новые иконки.
  `mini_app/src/pages/Welcome.tsx`, `Feed.tsx`, `components/feed/CardView.tsx`,
  `components/Splash.tsx`, `components/icons.tsx`. _Сделано._
- **fix (важно):** гейт входа — `/` теперь проверяет `GET /profiles/me`; завершивших
  онбординг сразу ведёт в ленту, а не гоняет по анкете заново. Welcome → `/welcome`.
  `mini_app/src/pages/Entry.tsx` (новый), `App.tsx`, `api/hooks.ts`. _Сделано._
- **feat:** флаг `AUTO_APPROVE_PHOTOS` (default False) — в пилоте фото сразу
  `approved`, иначе лента остаётся пустой (нет UI модерации). `shared/config.py`,
  `api/routers/photos.py`. _Сделано. Требует env `AUTO_APPROVE_PHOTOS=true` в Render._

---

## 💡 Идеи на будущее (реализовывать только после одобрения владельца)

| # | Идея | Зачем | Статус |
|---|------|-------|--------|
| 1 | Настроить **Cloudflare R2** (бакет + ключи + CORS), прописать `S3_*` в Render | Без хранилища фото не грузятся → регистрацию не завершить (блокер) | 🚧 в работе |
| 2 | Полноценная **модерация фото**: эндпоинт `GET /admin/photos?status=pending` + уведомление админу в бот + мини-экран модерации | Перед публичным запуском авто-одобрение надо выключить | 💡 предложено |
| 3 | **Ротация `BOT_TOKEN`** в BotFather перед публичным запуском | Текущий токен светился в переписке | 💡 предложено |
| 4 | Решить **cold start** на Render free (засыпает через 15 мин, ~50 сек на пробуждение): keep-alive пинг или платный тариф | Первый заход пользователя не должен висеть минуту | 💡 предложено |
| 5 | Дальнейший **UX-полир**: экран матча, пустые состояния, онбординг, микроанимации | Ощущение «душевности», ради которого затевалось | 💡 предложено |

---

_Формат записи в журнал:_ `**тип:** что сделано. затронутые файлы. статус.`
_Тип: `feat` (новое) · `fix` (баг) · `chore` (рутина) · `docs` · `refactor`._
