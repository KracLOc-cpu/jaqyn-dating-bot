{
  "buildCommand": "cd mini_app && npm install && npm run build",
  "outputDirectory": "mini_app/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
